# Historical Azure artifacts

The [`csvs/`](csvs/) directories are pre-audit Azure outputs retained for historical review. They are non-authoritative: they are not generated by the deterministic fixture, do not prove a current Azure cloud run, and must not be used as cross-platform expected results.

The authoritative local expectation is [`contracts/expected/baseline_snapshot.json`](../../../contracts/expected/baseline_snapshot.json).
